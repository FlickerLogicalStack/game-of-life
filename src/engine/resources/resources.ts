// Reserved potential functionality: GoL needs no game resources yet, so this stays a stub.
// The engine loop already accepts a `load_resources` callback; when sprites/images are needed,
// load them here and expose them through `engine.resources`.
export const load_resources = async () => ({});
